# Product Photo App Prototype

Small static prototype for the MVP flow.

## Run

Open `index.html` in a browser.

You can also run a local server:

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`.
